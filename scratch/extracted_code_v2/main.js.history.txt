=== HISTORY for main.js ===
Total edits: 6


============================================================
STEP=2220 | TOOL=replace:"
        w.innerHTML = (on ? 'â¥ ÄÃ£ lÆ°u' : 'â¡ YÃªu th
DESC="Fixing favorite activity tracking in main.js."
CONTENT:
"
        // Track activity for quests
        if (window.WanderUI) window.WanderUI.trackActivity('favorite');

        w.innerHTML = (on ? 'â¥ ÄÃ£ lÆ°u' : 'â¡ YÃªu thÃ­ch') + (w.querySelector('.wish-count') ? ' <span class="wish-count">' + (w.que
<truncated 75 bytes>

============================================================
STEP=2284 | TOOL=replace:"          fetch('/api/vision/search', {
            method:
DESC="Fixing broken visual search loop and improving scanner logic."
CONTENT:
"          fetch('/api/vision/search', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ image: base64 })
          })
          .then(function(res) { return res.json(); })
 
<truncated 2168 bytes>

============================================================
STEP=2566 | TOOL=replace:"          // Dynamic messages
          var msgs = [
      
DESC="Improved visual search logic in main.js with better status messages and result rendering."
CONTENT:
"          // Dynamic messages
          var msgs = [
            "Äang trÃ­ch xuáº¥t Äáº·c trÆ°ng hÃ¬nh áº£nh...",
            "PhÃ¢n tÃ­ch cáº£nh quan & kiáº¿n trÃºc...",
            "So khá»p cÆ¡ sá» dá»¯ liá»u Äá»a danh...",
         
<truncated 3719 bytes>

============================================================
STEP=2572 | TOOL=replace:"                // Track activity for quests
              
DESC="Cleaned up visual search result handling and scrolling logic in main.js."
CONTENT:
"                if (window.WanderUI) window.WanderUI.trackActivity('search');
                
                // Scroll to results if not visible
                var dropRect = searchDropdown.getBoundingClientRect();
                if (dropRect.bott
<truncated 778 bytes>

============================================================
STEP=2578 | TOOL=replace:"                if (window.WanderUI) window.WanderUI.trackA
DESC="Cleaned up visual search status messages and scrolling logic in main.js."
CONTENT:
"                // Scroll to results if not visible
                var dropRect = searchDropdown.getBoundingClientRect();
                if (dropRect.bottom > window.innerHeight) {
                  window.scrollBy({ top: dropRect.bottom - window.inn
<truncated 614 bytes>

============================================================
STEP=2581 | TOOL=replace:"                searchInput.focus();

                // Tr
DESC="Restored activity tracking for visual search in main.js."
CONTENT:
"                searchInput.focus();

                // Track activity for quests
                if (window.WanderUI) window.WanderUI.trackActivity('search');

                // Scroll to results if not visible"
